# @package _global_
defaults:
  - override /hydra/sweeper: optuna

hydra:
  sweeper:
    sampler:
      seed: 42
    direction: minimize
    study_name: base-patch16
    storage: null
    n_trials: 20
    n_jobs: 1
    max_failure_rate: 0.0
    params:
      train.commit_loss_weight: tag(log, interval(1e-7, 1e0))
      train.entropy_sample_minimization_weight: tag(log, interval(1e-5, 10))
      train.entropy_batch_maximization_weight: tag(log, interval(1e-5, 1))
      train.ema.beta: tag(log, interval(0.9, 0.999))
      train.optimizer.args.lr: tag(log,interval(1e-5, 1e-3))
      train.max_steps: 101
      train.eval_every_num_steps: 100
      model.lfq.temperature: tag(log, interval(1e-3, 1e0))
    
